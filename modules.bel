(form snarf ((e) a s r (p g))
  (let d (table (drain g [id _ e]))
    (mev s (cons d r) (p g))))

(set belext "bel")

(def modulefile (name|symbol)
  (append (nom name) "." belext))

(mac import (name . body)
  `(let g globe
     (load (modulefile name))
     (snarf g)))

